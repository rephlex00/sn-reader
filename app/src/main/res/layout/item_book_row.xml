<?xml version="1.0" encoding="utf-8"?>
<!-- One book as a single full-width list row: title, optional author, size, and a status badge
     (In progress / Not started / Unreadable: reason). No cover ImageView at all — list mode must
     never touch the cover cache or launch a decode (see BookGridAdapter). No animation-bearing
     attributes; the RecyclerView runs with itemAnimator = null (see LibraryActivity). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <TextView
        android:id="@+id/title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/author"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/reader_text_secondary"
        android:textSize="13sp" />

    <TextView
        android:id="@+id/size"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxLines="1"
        android:textColor="@color/reader_text_secondary"
        android:textSize="12sp" />

    <TextView
        android:id="@+id/status"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textColor="@color/reader_text_secondary"
        android:textSize="12sp" />

</LinearLayout>
