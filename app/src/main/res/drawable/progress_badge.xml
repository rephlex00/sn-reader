<?xml version="1.0" encoding="utf-8"?>
<!-- The library grid's reading-progress badge: white fill, hairline black border, black text.
     NOT a black fill — the badge sits over unpredictable cover artwork, and white-with-border
     stays legible over any shade while matching the app's chrome idiom. No gradient, no shadow:
     both ghost on e-ink. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/paper" />
    <stroke
        android:width="1dp"
        android:color="@color/ink" />
    <corners android:radius="3dp" />
</shape>
